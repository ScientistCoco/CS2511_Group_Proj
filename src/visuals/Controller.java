package visuals;

/**
 * A general interface for the controller class. This allows for models to add the controller and update them if required
 * @author court
 *
 */
public interface Controller {
	public void updateController();
}
